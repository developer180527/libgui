[package]
name = "libgui_nodes"
version = "0.1.0"
edition = "2021"
description = "Node-graph editing for libgui: nodes, ports, links, selection and link routing."

[dependencies]
libgui = { path = "../libgui" }
