[package]
name = "libgui_units"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
# No GPU crate here, so this is lower than the workspace floor.
rust-version = "1.87"
description = "An expression evaluator with units, for libgui's validated field: one answer for an app with no grammar of its own."

[dependencies]
libgui = { path = "../libgui" }
